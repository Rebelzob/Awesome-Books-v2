// Book class to represent a book
export default class Book {
  // Constructor takes a title, author, and id
  constructor(title, author, id) {
    this.title = title;
    this.author = author;
    this.id = id;
  }
}